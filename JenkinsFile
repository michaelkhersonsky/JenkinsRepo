pipeline {
    agent {
        docker {
            reuseNode true
            image 'centos'
           
          
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'cat /etc/os-release > /release.txt'
            }
        }
    }
}
