pipeline {
    agent {
        docker {
            image 'httpd'
          
        }
    }
    stages {
        stage('Build') {
            steps {
                bash 'httpd --help'
            }
        }
    }
}
