pipeline {
    agent {
        docker {
            image 'centos'
            
          
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'cat /etc/os-release'
            }
        }
    }
}
