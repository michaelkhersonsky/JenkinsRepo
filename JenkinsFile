pipeline {
    agent {
        docker {
            image 'httpd'
          
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'httdp --help'
            }
        }
    }
}
