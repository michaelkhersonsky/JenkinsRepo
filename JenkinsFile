pipeline {
    agent {
        docker {
            image 'httpd'
          
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'docker run -dt -p 5050:80 --name=my_container httpd'
            }
        }
    }
}
